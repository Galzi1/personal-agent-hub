---
phase: 00-validation-spikes
plan: 02
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - ".planning/phases/00-validation-spikes/00-02-SPIKE-RESULTS.md"
autonomous: false
requirements: []

must_haves:
  truths:
    - "Ollama is running locally and responds to model requests"
    - "A ~32B model (or appropriate alternative based on RAM) is pulled and operational"
    - "The model produces usable ranking output for AI news relevance"
    - "The model produces usable summarization output for AI news articles"
    - "The model produces usable 'why it matters' explanations"
    - "nomic-embed-text embedding model is pulled and produces vector output"
  artifacts:
    - path: ".planning/phases/00-validation-spikes/00-02-SPIKE-RESULTS.md"
      provides: "Ollama evaluation results with per-task pass/fail"
  key_links:
    - from: "Ollama runtime"
      to: "Qwen3 32B or Gemma 4 27B model"
      via: "ollama run CLI"
      pattern: "Model inference response"
---

<objective>
Evaluate whether a local Ollama model produces usable output for the 3 core tasks the project needs: ranking AI news by relevance, summarizing AI news articles, and generating "why it matters" explanations (per D-02). Also pull the embedding model for future use (per D-12).

Purpose: De-risk R3 (local Ollama model quality insufficient) before Phase 2 implementation commits to local-only LLM processing.
Output: Go/no-go decision for Ollama quality. If no-go, fallback is hybrid approach -- Ollama for embeddings, remote API for ranking/summarization (per D-05).
</objective>

<execution_context>
@C:/Users/galzi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/galzi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-validation-spikes/00-CONTEXT.md
@.planning/phases/00-validation-spikes/00-RESEARCH.md
@.planning/phases/00-validation-spikes/00-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pull Ollama models and run evaluation tasks</name>
  <files>.planning/phases/00-validation-spikes/00-02-SPIKE-RESULTS.md</files>
  <read_first>.planning/phases/00-validation-spikes/00-RESEARCH.md, .planning/phases/00-validation-spikes/00-01-SPIKE-RESULTS.md</read_first>
  <action>
**Step 1: Determine model based on system RAM (from Plan 01 results)**

Read the system RAM from 00-01-SPIKE-RESULTS.md or ask user if not recorded.
- If >= 32GB RAM: pull Qwen3 32B (per D-10, D-11 -- start with one model)
- If 16-31GB RAM: pull Gemma 4 27B MoE (smaller memory footprint, ~16GB at Q4)
- If < 16GB RAM: Note this as a constraint; try Gemma 4 27B anyway but expect issues

```bash
# For >= 32GB RAM:
ollama pull qwen3:32b

# For 16-31GB RAM:
ollama pull gemma4:27b
```

Also pull the embedding model (per D-12):
```bash
ollama pull nomic-embed-text
```

**Step 2: Verify models respond**
```bash
ollama list
# Should show the pulled chat model and nomic-embed-text

# Quick smoke test
ollama run <chosen-model> "Reply with exactly: MODEL OPERATIONAL"
```

**Step 3: Run 10-15 evaluation tasks (per D-02)**

Run each task via `ollama run <model>` CLI. Record the full output and latency for each.

**Ranking tasks (5 tasks):**

Task R1:
```
ollama run <model> "Given these 5 AI news items, rank them by importance for someone who builds AI-powered developer tools. Reply with the ranking (1=most important) and a one-sentence reason for each.

1. OpenAI releases GPT-5 with 2x reasoning improvement
2. A new VS Code extension adds AI-powered code review with inline suggestions
3. Python 3.14 adds experimental JIT compiler for 5x speedup
4. Google releases Gemini 2.5 Pro with 2M token context window
5. npm package downloads hit 1 trillion per month"
```

Task R2:
```
ollama run <model> "Rank these 4 AI news items by relevance for an AI tools enthusiast. Reply with ranking and one-sentence reason each.

1. Anthropic launches Claude 4 with improved agentic capabilities
2. Hugging Face acquires a small robotics startup
3. NVIDIA announces next-gen H200 GPU with 288GB HBM3e
4. A researcher publishes a paper on efficient attention mechanisms"
```

Task R3:
```
ollama run <model> "Rank these 3 items for someone tracking AI coding assistants specifically:

1. Cursor adds multi-file editing with background agents
2. Meta releases Llama 4 Scout with 10M token context
3. GitHub Copilot adds workspace-level understanding"
```

Task R4:
```
ollama run <model> "Which of these 4 items is LEAST relevant for someone who cares about practical AI tools (not research papers)?

1. New diffusion model achieves SOTA on ImageNet
2. Windsurf IDE adds AI terminal integration
3. DeepSeek publishes training methodology paper
4. Ollama adds native tool calling support"
```

Task R5:
```
ollama run <model> "Rank by urgency -- which of these would a developer want to know about TODAY vs. next week?

1. Critical security vulnerability found in popular AI SDK
2. New benchmark shows GPT-5 outperforms Claude on coding
3. Microsoft announces Copilot price increase effective next month
4. Research paper proposes new tokenization approach"
```

**Summarization tasks (5 tasks):**

Task S1:
```
ollama run <model> "Summarize this in 2-3 sentences for a developer audience: OpenAI has released GPT-5, their latest large language model featuring significantly improved reasoning capabilities, native multimodal understanding, and a new 'deep research' mode that can autonomously browse the web and synthesize findings. The model is available through the API at the same price point as GPT-4 Turbo, with a 256K token context window."
```

Task S2:
```
ollama run <model> "Summarize in 2-3 sentences: Cursor has launched a new background agent feature that can work on multiple files simultaneously while the developer continues coding. The agent operates in a sandboxed environment, makes changes across the codebase, runs tests, and presents a diff for review. Early users report 3-5x productivity gains on refactoring tasks."
```

Task S3:
```
ollama run <model> "Summarize in 2 sentences: Google DeepMind announced Gemini 2.5 Pro, a model with native audio, image, and video understanding plus a 2 million token context window. It achieves state-of-the-art results on 15 out of 20 standard benchmarks and is available immediately through the Gemini API and Google AI Studio."
```

Task S4:
```
ollama run <model> "Summarize in 2-3 sentences: Anthropic has open-sourced their Model Context Protocol (MCP), a standard for connecting AI assistants to external data sources and tools. MCP provides a universal interface that replaces custom integrations, similar to how USB-C standardized device connectivity. Over 50 companies have already adopted MCP in their products."
```

Task S5:
```
ollama run <model> "Summarize in 1-2 sentences: The latest npm audit report shows that 47% of JavaScript projects have at least one known vulnerability in their dependency tree, up from 38% last year. The report recommends automated dependency updates and regular audits."
```

**"Why it matters" tasks (3-5 tasks):**

Task W1:
```
ollama run <model> "In 2 sentences, explain why this matters for someone who builds with AI tools: Ollama has added native tool calling support, allowing local models to invoke external functions and APIs during inference."
```

Task W2:
```
ollama run <model> "In 2 sentences, explain why this matters for a developer: GitHub Copilot now understands your entire workspace structure, not just the current file, when making suggestions."
```

Task W3:
```
ollama run <model> "In 2 sentences, explain why this matters for someone tracking AI developments: A new open-source model with 7B parameters matches GPT-4 performance on coding benchmarks."
```

Task W4 (optional):
```
ollama run <model> "In 2 sentences, explain why this matters for an AI tools enthusiast: Discord has launched an AI-powered moderation system that can understand context and nuance in conversations."
```

Task W5 (optional):
```
ollama run <model> "In 2 sentences, explain why this matters: SQLite has added built-in vector search support, eliminating the need for external vector databases for small-to-medium workloads."
```

**Step 4: Test embedding model**
```bash
# Verify nomic-embed-text produces embeddings
ollama run nomic-embed-text "Test embedding for AI news categorization"
# Or via API:
curl http://localhost:11434/api/embed -d '{"model":"nomic-embed-text","input":"Test embedding for AI news"}'
```
Confirm it returns a vector (array of floats). Note the dimension count (should be 768).

**Step 5: Record all results**

Create `.planning/phases/00-validation-spikes/00-02-SPIKE-RESULTS.md` containing:

```markdown
# Ollama Model Quality Evaluation Results

## Model Tested
- Chat/Ranking: [model name and size]
- Embedding: nomic-embed-text
- System RAM: [amount from Plan 01]
- Inference mode: CPU-only (AMD Radeon 890M, 512MB VRAM)

## Ranking Tasks

| Task | Latency | Output Quality | Pass/Fail | Notes |
|------|---------|---------------|-----------|-------|
| R1 | Xs | [brief assessment] | PASS/FAIL | |
| R2 | Xs | | | |
| R3 | Xs | | | |
| R4 | Xs | | | |
| R5 | Xs | | | |

## Summarization Tasks

| Task | Latency | Output Quality | Pass/Fail | Notes |
|------|---------|---------------|-----------|-------|
| S1 | Xs | | | |
| ... | | | | |

## Why It Matters Tasks

| Task | Latency | Output Quality | Pass/Fail | Notes |
|------|---------|---------------|-----------|-------|
| W1 | Xs | | | |
| ... | | | | |

## Embedding Model
- Produces vectors: YES/NO
- Dimension count: [N]
- Notes: [any issues]

## Summary
- Total tasks: [N]
- Passed: [N]
- Failed: [N]
- Average latency: [X]s
- Go/No-Go: [GO/NO-GO]

## Fallback Assessment (if NO-GO)
Per D-05: Keep Ollama for embeddings, add remote provider for ranking/summarization.
Per D-11: If first model fails, consider testing [alternative model] before declaring NO-GO.

## Notes for Phase 1+
[Observations about quality, latency, specific strengths/weaknesses]
```
  </action>
  <verify>
    <automated>MISSING -- Ollama evaluation requires human quality judgment. Verify by reading: cat .planning/phases/00-validation-spikes/00-02-SPIKE-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
- File `.planning/phases/00-validation-spikes/00-02-SPIKE-RESULTS.md` exists
- File contains a results table with at least 10 tasks evaluated
- Each task row has a Latency value and a Pass/Fail judgment
- File contains "Go/No-Go:" line matching "(GO|NO-GO)"
- File contains "Embedding Model" section with vector confirmation
- `ollama list` shows both the chat model and nomic-embed-text as pulled
  </acceptance_criteria>
  <done>10-15 Ollama tasks evaluated with per-task pass/fail, embedding model verified, overall quality assessment recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ollama quality go/no-go decision</name>
  <files>.planning/phases/00-validation-spikes/00-02-SPIKE-RESULTS.md</files>
  <read_first>.planning/phases/00-validation-spikes/00-02-SPIKE-RESULTS.md</read_first>
  <action>
Present the spike results from 00-02-SPIKE-RESULTS.md to the user for go/no-go verification.

How to verify:
1. Review `.planning/phases/00-validation-spikes/00-02-SPIKE-RESULTS.md`
2. For each task output, judge: is this usable quality for a daily AI digest? (per D-02)
   - Ranking: Does the model rank items sensibly? Are the reasons coherent?
   - Summarization: Are summaries accurate and concise? No hallucinations?
   - Why it matters: Are explanations relevant and insightful?
3. Check latency: Is per-task latency acceptable for a daily batch process? (Minutes OK for daily digest, 10+ minutes per task is concerning)
4. Majority pass = GO. Majority fail = consider D-11 (test alternative model) or D-05 (hybrid fallback)
5. Verify embedding model produces vectors (needed for Phase 2 semantic dedup)

Resume signal: Type "GO" to proceed to watchlist backtest (Plan 03), "TEST-ALT" to try the alternative model (per D-11), or "NO-GO" to adopt hybrid fallback (per D-05).
  </action>
  <verify>
    <automated>cat .planning/phases/00-validation-spikes/00-02-SPIKE-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
- 00-02-SPIKE-RESULTS.md contains "Go/No-Go:" line with either "GO" or "NO-GO"
- User has confirmed their quality judgment of model outputs
  </acceptance_criteria>
  <done>User has reviewed Ollama evaluation results and confirmed GO, TEST-ALT, or NO-GO decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ollama local runtime | All inference is local, no data leaves the machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03 | Information Disclosure | Ollama model downloads | accept | Models are downloaded from ollama.com registry over HTTPS. No user data is uploaded. Standard package download risk. |
| T-00-04 | Denial of Service | Ollama memory consumption | accept | CPU-only inference on limited RAM may cause system slowness. Mitigated by model size selection based on available RAM. Not a security threat, operational concern only. |
</threat_model>

<verification>
- `ollama list` shows both the chat model and nomic-embed-text
- Spike results file exists with 10+ task evaluations
- Each task has a recorded latency and pass/fail judgment
- Go/no-go decision is documented with rationale
</verification>

<success_criteria>
Go/no-go decision for Ollama model quality recorded with evidence from 10-15 evaluation tasks. If GO, local Ollama is confirmed for ranking/summarization. If NO-GO, hybrid fallback path (D-05) is documented.
</success_criteria>

<output>
After completion, create `.planning/phases/00-validation-spikes/00-02-SUMMARY.md`
</output>
