---
phase: 02-canonical-story-formation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - package.json
  - src/lib/cognee.ts
  - src/api/story/recall/route.ts
autonomous: true
requirements:
  - SIG-01
  - SIG-04
must_haves:
  truths:
    - "System uses Cognee Knowledge Engine for story memory"
    - "User can recall canonical stories with uncertainty feedback"
  artifacts:
    - path: "src/lib/cognee.ts"
      provides: "Cognee integration client and helper functions"
    - path: "src/api/story/recall/route.ts"
      provides: "Recall API endpoint for stories with confidence data"
  key_links:
    - from: "src/api/story/recall/route.ts"
      to: "src/lib/cognee.ts"
      via: "recall call to Cognee engine"
      pattern: "cognee.recall"
    - from: "src/lib/cognee.ts"
      to: "Cognee API"
      via: "HTTP request or client library"
      pattern: "fetch.*cognee\\.ai"
---

<objective>
Integrate the Cognee Knowledge Engine and expose a story recall API.

Purpose: Enhance story memory with graph-based search and provide uncertainty-aware story retrieval.
Output: Cognee client library integration and a story recall REST API.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/phases/02-canonical-story-formation/02-01-PLAN-SUMMARY.md
</execution_context>

<context>
@.planning/phases/02-canonical-story-formation/02-canonical-story-formation-DISCOVERY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure Cognee client</name>
  <files>package.json, src/lib/cognee.ts</files>
  <action>Install `cognee` (using npm or similar). Create `src/lib/cognee.ts` and initialize the Cognee client. Provide helper functions:
- `rememberStory(story: Story): Promise<void>`: Stores a story in the knowledge graph.
- `recallStories(query: string): Promise<Story[]>`: Performs a recall call on the graph.
Use environment variables for API keys and endpoint URLs.
</action>
  <verify>
    <automated>rtk grep "cognee" package.json && rtk grep "export.*recall" src/lib/cognee.ts</automated>
  </verify>
  <done>Cognee client initialized and helper functions exported.</done>
</task>

<task type="auto">
  <name>Task 2: Expose Story Recall API</name>
  <files>src/api/story/recall/route.ts</files>
  <action>Create a GET endpoint `/api/story/recall` that accepts a `query` parameter.
Call `cognee.recall` helper function.
Format the output to include story title, content, confidence, and Cognee-provided search results.
</action>
  <verify>
    <automated>curl "http://localhost:3000/api/story/recall?query=test" | grep \"title\"</automated>
  </verify>
  <done>GET endpoint returns relevant canonical stories with uncertainty features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→Cognee Cloud | Sensitive story data is transmitted to an external service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Cognee API request | mitigate | Ensure TLS 1.2+ for all API calls; use signed requests if supported |
| T-02-04 | Denial of Service | `/api/story/recall` | mitigate | Implement rate limiting for API endpoint and Cognee calls |
</threat_model>

<verification>
Overall phase checks: Verify Cognee client successfully connects and recall API returns results matching stored stories.
</verification>

<success_criteria>
- Cognee integration successfully persists and retrieves story data.
- Story recall API correctly formats results with confidence metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-story-formation/02-02-PLAN-SUMMARY.md`
</output>
