---
phase: 01-ingestion-foundation-run-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - .env.example
  - microclaw.config.yaml.example
  - src/personal_agent_hub/__init__.py
  - src/personal_agent_hub/settings.py
  - src/personal_agent_hub/persistence/db.py
  - tests/conftest.py
autonomous: true
requirements:
  - SRC-01
  - DGST-04
user_setup:
  - service: discord
    why: "Phase 1 status delivery needs a real Discord bot token and the dedicated status-channel ID."
    env_vars:
      - name: DISCORD_BOT_TOKEN
        source: "Discord Developer Portal -> Applications -> Bot -> Token"
      - name: DISCORD_STATUS_CHANNEL_ID
        source: "Discord client -> Copy Channel ID for the dedicated agent-status channel"
must_haves:
  truths:
    - "A new local Windows checkout can bootstrap Phase 1 with local-only defaults for MicroClaw, Ollama, and Discord status delivery."
    - "The shared runtime baseline keeps control-panel access and status-channel configuration local-only until real credentials are supplied."
    - "Phase 1 intake and run-visibility behavior can be validated repeatably from one shared pytest harness."
  artifacts:
    - path: "pyproject.toml"
      provides: "uv-managed Python package and pytest bootstrap"
      contains: "feedparser==6.0.12"
    - path: "microclaw.config.yaml.example"
      provides: "Local-only MicroClaw + Discord + Ollama config template"
      contains: "llm_base_url: http://127.0.0.1:11434/v1"
    - path: "tests/conftest.py"
      provides: "Shared Phase 1 pytest settings/bootstrap helpers"
      contains: "build_test_settings"
  key_links:
    - from: ".env.example"
      to: "microclaw.config.yaml.example"
      via: "shared Discord/Ollama settings"
      pattern: "DISCORD_BOT_TOKEN|DISCORD_STATUS_CHANNEL_ID|http://127.0.0.1:11434/v1|http://127.0.0.1:10961/runs"
    - from: "tests/conftest.py"
      to: "src/personal_agent_hub/settings.py"
      via: "shared test settings/bootstrap"
      pattern: "AppSettings|build_test_settings"
---

<objective>
Bootstrap the empty repo into a runnable Phase 1 workspace with Windows-first Python tooling, local-only runtime templates, and the shared pytest harness later plans will extend.

Purpose: Remove all greenfield ambiguity before source intake and run-visibility code begins.
Output: Python project scaffold, local config templates, shared settings/database helpers, and a reusable pytest bootstrap for SRC-01 and DGST-04 tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ingestion-foundation-run-visibility/01-CONTEXT.md
@.planning/phases/01-ingestion-foundation-run-visibility/01-RESEARCH.md
@copilot-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 1 Python/runtime bootstrap and local-only config templates</name>
  <read_first>
    - pyproject.toml
    - .gitignore
    - .env.example
    - microclaw.config.yaml.example
    - src/personal_agent_hub/settings.py
    - src/personal_agent_hub/persistence/db.py
    - .planning/phases/01-ingestion-foundation-run-visibility/01-CONTEXT.md
    - .planning/phases/01-ingestion-foundation-run-visibility/01-RESEARCH.md
  </read_first>
  <files>pyproject.toml, .gitignore, .env.example, microclaw.config.yaml.example, src/personal_agent_hub/__init__.py, src/personal_agent_hub/settings.py, src/personal_agent_hub/persistence/db.py</files>
  <action>Create a uv-managed Python package named `personal-agent-hub` with source root `src/personal_agent_hub`. In `pyproject.toml`, set `requires-python = ">=3.12,<3.14"` and pin exact runtime/test dependencies `feedparser==6.0.12`, `httpx==0.28.1`, `pydantic==2.12.5`, and `pytest==9.0.3`; expose a console script `pah = "personal_agent_hub.cli:main"` even though `cli.py` is implemented later. In `.gitignore`, add `.venv/`, `.env`, `data/`, `.pytest_cache/`, `__pycache__/`, and `microclaw.config.yaml` so Discord secrets and runtime DB files never enter git (mitigates Discord token leakage). In `.env.example`, define exactly `PAH_DB_PATH=./data/personal_agent_hub.sqlite3`, `PAH_WATCHLIST_PATH=./config/watchlist.yaml`, `PAH_TRACE_BASE_URL=http://127.0.0.1:10961/runs`, `OLLAMA_BASE_URL=http://127.0.0.1:11434/v1`, `DISCORD_BOT_TOKEN=`, and `DISCORD_STATUS_CHANNEL_ID=` so later plans can build the exact D-13 raw-event link `http://127.0.0.1:10961/runs/<run_id>/events`. In `microclaw.config.yaml.example`, set `web_enabled: true`, `llm_provider: ollama`, `llm_base_url: http://127.0.0.1:11434/v1`, `embedding_provider: ollama`, `allow_group_slash_without_mention: false`, `working_dir_isolation: shared`, and a Discord multi-account block using `channels.discord.default_account: main` with `channels.discord.accounts.main.bot_token: "${DISCORD_BOT_TOKEN}"` and `channels.discord.accounts.main.allowed_channels: ["${DISCORD_STATUS_CHANNEL_ID}"]`; do not add any non-local bind host or public port configuration so the control panel stays on `127.0.0.1` only (per D-08 and D-13). In `src/personal_agent_hub/settings.py`, implement a typed `AppSettings` loader for the exact env vars above. In `src/personal_agent_hub/persistence/db.py`, implement a shared SQLite connection helper that creates the parent `data/` directory, enables foreign keys, and applies WAL mode so later plans can add raw item, run, and event tables without redefining connection behavior.</action>
  <acceptance_criteria>
    - `rg -n "feedparser==6.0.12|httpx==0.28.1|pydantic==2.12.5|pytest==9.0.3" pyproject.toml` returns all 4 pins.
    - `rg -n "PAH_TRACE_BASE_URL=http://127.0.0.1:10961/runs" .env.example` returns 1 match.
    - `rg -n "allowed_channels" microclaw.config.yaml.example` returns 1 match and references `${DISCORD_STATUS_CHANNEL_ID}`.
    - `rg -n "PRAGMA foreign_keys = ON|journal_mode=WAL" src/personal_agent_hub/persistence/db.py` returns 2 matches.
    - `rg -n "^\\.env$|^data/$|^microclaw\\.config\\.yaml$" .gitignore` returns 3 matches.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest --version</automated>
  </verify>
  <done>Tooling installs from `pyproject.toml`, secrets/runtime files are ignored, and local-only runtime templates exist for MicroClaw + Ollama + Discord status delivery.</done>
</task>

<task type="auto">
  <name>Task 2: Create the shared Phase 1 pytest/bootstrap harness</name>
  <read_first>
    - tests/conftest.py
    - src/personal_agent_hub/settings.py
    - src/personal_agent_hub/persistence/db.py
    - .planning/phases/01-ingestion-foundation-run-visibility/01-CONTEXT.md
  </read_first>
  <files>tests/conftest.py</files>
  <action>Create `tests/conftest.py` with helpers that build a temporary SQLite path, temporary watchlist path, and shared `AppSettings` for test runs. The helper contract must expose a reusable `build_test_settings(...)` path that defaults `trace_base_url` to `http://127.0.0.1:10961/runs` so later phase-owned tests can verify the exact D-13 route `http://127.0.0.1:10961/runs/<run_id>/events`. Keep this task limited to shared bootstrap only; move requirement-specific fixtures and test files into the plans that already own those surfaces so Plan 01 stays within a saner scope.</action>
  <acceptance_criteria>
    - `rg -n "build_test_settings|tmp_path" tests/conftest.py` returns both matches.
    - `rg -n "http://127.0.0.1:10961/runs" tests/conftest.py` returns 1 match.
    - `rg -n "AppSettings" tests/conftest.py` returns at least 1 match.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from pathlib import Path; text = Path('tests/conftest.py').read_text(); assert 'build_test_settings' in text and 'http://127.0.0.1:10961/runs' in text"</automated>
  </verify>
  <done>The repo has one shared pytest/bootstrap entrypoint that later plans can reuse when they create the SRC-01 and DGST-04 requirement tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local env -> config loader | Secrets and local runtime paths enter the app from environment/template files |
| shared test harness -> later phase tests | Temporary paths and trace-base defaults flow into requirement-owned test modules |
| local browser -> MicroClaw Web UI | Control-panel access must remain localhost-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `tests/conftest.py` + future requirement tests | mitigate | Keep bootstrap helpers limited to local temp paths/settings only, then enforce allowlisted watchlist URLs and parser validation in later ingestion code so malformed input never turns the shared harness into an unsafe shortcut. |
| T-01-02 | I | `microclaw.config.yaml.example` | mitigate | Keep `web_enabled: true` but do not add any public bind/proxy settings; all plan actions use `127.0.0.1` trace URLs only. |
| T-01-03 | E | `.env.example` / `.gitignore` | mitigate | Template `DISCORD_BOT_TOKEN` only, never a real token; ignore `.env` and `microclaw.config.yaml` so live credentials cannot be committed. |
</threat_model>

<verification>
- `uv run pytest --version`
- `uv run python -c "from pathlib import Path; text = Path('tests/conftest.py').read_text(); assert 'build_test_settings' in text and 'http://127.0.0.1:10961/runs' in text"`
</verification>

<success_criteria>
- A new checkout can install the declared Python dependencies with uv.
- Local-only MicroClaw/Ollama/Discord templates exist without any hard-coded secrets.
- The shared Phase 1 pytest harness exists so later plans can add requirement-owned tests and fixtures without redoing bootstrap.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-foundation-run-visibility/01-ingestion-foundation-run-visibility-01-SUMMARY.md`
</output>
