---
phase: 01-ingestion-foundation-run-visibility
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/personal_agent_hub/cli.py
  - src/personal_agent_hub/workflows/daily_digest_run.py
  - src/personal_agent_hub/visibility/discord_status.py
  - microclaw.config.yaml.example
  - scripts/register_phase1_schedule.ps1
  - scripts/bootstrap_microclaw_runtime.ps1
  - tests/test_daily_digest_run.py
  - tests/test_discord_status.py
autonomous: true
requirements:
  - SRC-01
  - DGST-04
user_setup:
  - service: discord
    why: "A real Discord bot token and the dedicated status-channel ID are required before MicroClaw can post milestone narration."
    env_vars:
      - name: DISCORD_BOT_TOKEN
        source: "Discord Developer Portal -> Applications -> Bot -> Token"
      - name: DISCORD_STATUS_CHANNEL_ID
        source: "Discord client -> Copy Channel ID for the dedicated agent-status channel"
must_haves:
  truths:
    - "A daily Phase 1 run can start, ingest from the watchlist, and finish with one explicit terminal outcome."
    - "Discord-visible status narration covers only start, ingestion complete, and final outcome in the dedicated status channel."
    - "Each visible status message names the exact run label/short ID and links back to that run's raw event/log view."
    - "A live local MicroClaw + Ollama foundation can pass health checks and execute one Phase 1 smoke run."
  artifacts:
    - path: "src/personal_agent_hub/workflows/daily_digest_run.py"
      provides: "End-to-end Phase 1 run coordinator"
      contains: "trigger_kind"
    - path: "src/personal_agent_hub/visibility/discord_status.py"
      provides: "Three-message narration policy"
      contains: "run start"
    - path: "scripts/register_phase1_schedule.ps1"
      provides: "08:00 local MicroClaw schedule bootstrap"
      contains: "0 0 8 * * *"
    - path: "scripts/bootstrap_microclaw_runtime.ps1"
      provides: "Live MicroClaw + Ollama foundation bootstrap and smoke verification"
      contains: "PHASE1_SMOKE_RUN=ok"
    - path: "tests/test_daily_digest_run.py"
      provides: "Coordinator and CLI integration proof"
      contains: "test_scheduled_failure_retries_once_after_10_minutes"
    - path: "tests/test_discord_status.py"
      provides: "Discord milestone rendering proof"
      contains: "test_renders_three_separate_milestone_posts"
  key_links:
    - from: "src/personal_agent_hub/workflows/daily_digest_run.py"
      to: "src/personal_agent_hub/visibility/discord_status.py"
      via: "render_status_message"
      pattern: "render_status_message|post_status|run.started|ingestion.completed|run.(completed|no_items|failed)"
    - from: "scripts/register_phase1_schedule.ps1"
      to: "microclaw.config.yaml.example"
      via: "shared MicroClaw runtime config"
      pattern: "microclaw.config.yaml"
---

<objective>
Wire the Phase 1 workflow into a runnable coordinator with retry behavior and Discord status narration, then register the exact 08:00 local schedule.

Purpose: This is the plan that turns the watchlist + run ledger into one visible MicroClaw-driven workflow.
Output: End-to-end CLI/workflow entrypoint, Discord milestone renderer, local-only MicroClaw config update, live runtime bootstrap/smoke verification, schedule-registration script, and task-owned workflow/status verification tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ingestion-foundation-run-visibility/01-CONTEXT.md
@.planning/phases/01-ingestion-foundation-run-visibility/01-RESEARCH.md
@.planning/phases/01-ingestion-foundation-run-visibility/01-02-PLAN.md
@.planning/phases/01-ingestion-foundation-run-visibility/01-03-PLAN.md

<interfaces>
Expect `load_watchlist(path)` from `src/personal_agent_hub/sources/watchlist.py`, raw-item helpers from `src/personal_agent_hub/persistence/raw_items.py`, and `create_run(...)`, `create_child_run(...)`, `append_event(...)`, `build_trace_url(...)` from the run modules created in Plans 02-03.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the end-to-end Phase 1 run coordinator with retry and manual rerun behavior</name>
  <read_first>
    - src/personal_agent_hub/cli.py
    - src/personal_agent_hub/workflows/daily_digest_run.py
    - src/personal_agent_hub/sources/watchlist.py
    - src/personal_agent_hub/sources/feed_ingestor.py
    - src/personal_agent_hub/runs/repository.py
    - src/personal_agent_hub/runs/events.py
    - src/personal_agent_hub/visibility/discord_status.py
    - tests/test_daily_digest_run.py
    - tests/test_ingestion_multisource.py
    - tests/test_run_visibility.py
    - tests/test_run_lineage.py
  </read_first>
  <files>src/personal_agent_hub/cli.py, src/personal_agent_hub/workflows/daily_digest_run.py, tests/test_daily_digest_run.py</files>
  <behavior>
    - Test 1: `pah run --trigger scheduled` creates a run, ingests enabled sources, writes `ingestion.completed`, and finishes as `completed_with_candidates` or `completed_no_items`.
    - Test 2: a scheduled failure creates exactly one retry child run after 10 minutes; a second failure ends as `failed`.
    - Test 3: `pah run --trigger manual --parent-run-id <run_id>` creates a new manual child run instead of mutating the parent.
  </behavior>
  <action>Implement `src/personal_agent_hub/workflows/daily_digest_run.py` as the single Phase 1 coordinator per D-16 through D-18. The coordinator must: (1) create a run and append `run.started`; (2) load the Phase 1 watchlist; (3) fetch and persist raw items for all enabled sources; (4) append `ingestion.completed` with exact counters `source_count`, `source_successes`, `source_failures`, `raw_item_count`, and `candidate_count`; (5) select one terminal outcome only: `completed_with_candidates` when `candidate_count > 0`, `completed_no_items` when `candidate_count == 0`, or `failed` on unrecovered exceptions. For scheduled runs only, catch transient ingestion failures once, mark the original run `retry_pending`, append `retry.scheduled` with `retry_after = now + 10 minutes`, then create a single child run with `trigger_kind="retry"`; if that retry fails, mark the retry run `failed` and leave the parent linked, not overwritten. Implement `src/personal_agent_hub/cli.py` with a `run` subcommand accepting `--trigger scheduled|retry|manual` and optional `--parent-run-id`, plus `--scheduled-for` for deterministic tests. Create or update `tests/test_daily_digest_run.py` so it directly proves: a scheduled run records exactly one terminal outcome, a scheduled failure retries once after 10 minutes, and a manual rerun creates a child run instead of mutating its parent. Do not add ranking, story formation, digest composition, or feedback controls in this plan.</action>
  <acceptance_criteria>
    - `rg -n "source_count|source_successes|source_failures|raw_item_count|candidate_count" src/personal_agent_hub/workflows/daily_digest_run.py` returns all 5 counter names.
    - `rg -n "--trigger" src/personal_agent_hub/cli.py` returns the CLI argument definition.
    - `rg -n "retry.scheduled|completed_no_items|completed_with_candidates" src/personal_agent_hub/workflows/daily_digest_run.py` returns all 3 values.
    - `rg -n "test_scheduled_run_records_one_terminal_outcome|test_scheduled_failure_retries_once_after_10_minutes|test_manual_rerun_creates_child_run" tests/test_daily_digest_run.py` returns all 3 test names.
    - `uv run pytest tests/test_daily_digest_run.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_daily_digest_run.py -q</automated>
  </verify>
  <done>The repo has a single executable Phase 1 workflow entrypoint that creates runs, ingests from the watchlist, handles scheduled retry/manual rerun lineage, and has task-owned integration tests proving those behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Discord milestone narration and register the 08:00 MicroClaw schedule</name>
  <read_first>
    - src/personal_agent_hub/visibility/discord_status.py
    - microclaw.config.yaml.example
    - scripts/register_phase1_schedule.ps1
    - src/personal_agent_hub/workflows/daily_digest_run.py
    - tests/test_discord_status.py
    - .env.example
    - .planning/phases/01-ingestion-foundation-run-visibility/01-CONTEXT.md
    - .planning/phases/01-ingestion-foundation-run-visibility/01-RESEARCH.md
  </read_first>
  <files>src/personal_agent_hub/workflows/daily_digest_run.py, src/personal_agent_hub/visibility/discord_status.py, microclaw.config.yaml.example, scripts/register_phase1_schedule.ps1, tests/test_discord_status.py</files>
  <action>In `src/personal_agent_hub/visibility/discord_status.py`, implement exactly three milestone renderers per D-08 through D-11: `run started`, `ingestion complete`, and `final outcome`. Each renderer must target the dedicated status channel only, include the exact `run_label`, `short_run_id`, and raw-event `trace_url`, and use concise text: start = run label + trigger kind; ingestion complete = run label + `source_successes/source_count` + `raw_item_count`; final outcome = one of `completed_with_candidates`, `completed_no_items`, or `failed` plus a short reason and the exact `http://127.0.0.1:10961/runs/<run_id>/events` link. Then explicitly update `src/personal_agent_hub/workflows/daily_digest_run.py` so the workflow emits three separate milestone posts through `render_status_message(...)` + `post_status(...)`: immediately after `run.started`, immediately after `ingestion.completed`, and once after the final terminal outcome. Do not rely on in-place message edits; separate posts are the Phase 1 contract. Do not emit per-source chatter or any digest-channel message in Phase 1. Update `microclaw.config.yaml.example` so it still keeps `web_enabled: true`, `llm_provider: ollama`, `llm_base_url: http://127.0.0.1:11434/v1`, `embedding_provider: ollama`, `allow_group_slash_without_mention: false`, `channels.discord.default_account: main`, and `channels.discord.accounts.main.allowed_channels: ["${DISCORD_STATUS_CHANNEL_ID}"]`; do not loosen channel allowlisting or localhost-only control-panel posture. Create `scripts/register_phase1_schedule.ps1` that (a) copies `microclaw.config.yaml.example` to `microclaw.config.yaml` if the real file is missing, (b) verifies `http://127.0.0.1:10961` is reachable, and (c) sends an authenticated `POST` to `http://127.0.0.1:10961/api/chat` with `session_key` set to `phase1-status-scheduler`, `sender_name` set to `automation`, and a message that schedules the exact cron `0 0 8 * * *` to run `uv run pah run --trigger scheduled`. The same script must print the exact fallback prompt to paste into the Discord status channel if the HTTP API is not yet authenticated: `Every day at 08:00 local, run 'uv run pah run --trigger scheduled' and narrate only run start, ingestion complete, and final outcome for the dedicated agent-status channel.` Create or update `tests/test_discord_status.py` so it directly proves the renderer emits three separate milestone posts, targets the dedicated status channel only, and formats trace URLs as `http://127.0.0.1:10961/runs/<run_id>/events`.</action>
  <acceptance_criteria>
    - `rg -n "run started|ingestion complete|final outcome" src/personal_agent_hub/visibility/discord_status.py` returns 3 matches.
    - `rg -n "render_status_message|post_status" src/personal_agent_hub/workflows/daily_digest_run.py` returns at least 2 matches.
    - `rg -n "allowed_channels" microclaw.config.yaml.example` returns 1 match and only references `${DISCORD_STATUS_CHANNEL_ID}`.
    - `rg -n "0 0 8 \\* \\* \\*" scripts/register_phase1_schedule.ps1` returns 1 match.
    - `rg -n "/api/chat|phase1-status-scheduler|uv run pah run --trigger scheduled" scripts/register_phase1_schedule.ps1` returns all 3 values.
    - `rg -n "test_renders_three_separate_milestone_posts|test_targets_status_channel_only|test_trace_url_points_to_raw_event_view" tests/test_discord_status.py` returns all 3 test names.
    - `uv run pytest tests/test_discord_status.py -q` exits 0.
    - `pwsh -NoProfile -File scripts/register_phase1_schedule.ps1 -WhatIf` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_discord_status.py -q && pwsh -NoProfile -File scripts/register_phase1_schedule.ps1 -WhatIf</automated>
  </verify>
  <done>Phase 1 has a local-only MicroClaw + Discord configuration, exactly three separate status milestone posts, exact raw-event traceability in every visible message, an automatable 08:00 local schedule registration path, and task-owned proof for both renderer behavior and script validity.</done>
</task>

<task type="auto">
  <name>Task 3: Stand up and verify the live MicroClaw + Ollama runtime foundation</name>
  <read_first>
    - scripts/bootstrap_microclaw_runtime.ps1
    - microclaw.config.yaml.example
    - .env.example
    - src/personal_agent_hub/workflows/daily_digest_run.py
    - src/personal_agent_hub/visibility/discord_status.py
    - .planning/phases/01-ingestion-foundation-run-visibility/01-RESEARCH.md
    - .planning/todos/pending/setup-microclaw-ollama-prototype.md
  </read_first>
  <files>scripts/bootstrap_microclaw_runtime.ps1</files>
  <action>Create `scripts/bootstrap_microclaw_runtime.ps1` as the concrete live-runtime bootstrap and smoke-verification entrypoint for the folded MicroClaw + Ollama prototype work. The script must: (1) fail fast unless `ollama` is installed and `http://127.0.0.1:11434/api/tags` responds successfully; (2) copy `microclaw.config.yaml.example` to `microclaw.config.yaml` if the live file is missing; (3) start MicroClaw from a caller-supplied `-MicroClawBinPath` when `http://127.0.0.1:10961` is not yet reachable; (4) poll until the local Web UI/control plane is healthy on `http://127.0.0.1:10961`; (5) run one smoke command `uv run pah run --trigger manual --scheduled-for 2026-04-11T08:00:00`; and (6) print the exact markers `OLLAMA_HEALTH=ok`, `MICROCLAW_HEALTH=ok`, and `PHASE1_SMOKE_RUN=ok` only after the corresponding checks pass. Do not target any non-local host, do not add fallback cloud providers, and do not treat example config alone as sufficient completion.</action>
  <acceptance_criteria>
    - `rg -n "127\\.0\\.0\\.1:11434/api/tags|127\\.0\\.0\\.1:10961|uv run pah run --trigger manual --scheduled-for 2026-04-11T08:00:00" scripts/bootstrap_microclaw_runtime.ps1` returns 3 matches.
    - `rg -n "OLLAMA_HEALTH=ok|MICROCLAW_HEALTH=ok|PHASE1_SMOKE_RUN=ok" scripts/bootstrap_microclaw_runtime.ps1` returns all 3 markers.
    - `pwsh -NoProfile -File scripts/bootstrap_microclaw_runtime.ps1 -MicroClawBinPath $env:MICROCLAW_BIN_PATH` exits 0 after proving Ollama health, MicroClaw health, and one manual Phase 1 smoke run.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File scripts/bootstrap_microclaw_runtime.ps1 -MicroClawBinPath $env:MICROCLAW_BIN_PATH</automated>
  </verify>
  <done>The local MicroClaw + Ollama foundation is live, health-checked, and proven to run one Phase 1 coordinator smoke flow instead of existing only as example config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow coordinator -> Discord status renderer | Run state crosses into an externally visible channel message |
| local script -> MicroClaw Web API | Automation registers a schedule through the local control-plane API |
| operator secrets -> runtime config | Discord bot token and status-channel ID enter the live MicroClaw config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | I | `discord_status.py` | mitigate | Render only the dedicated status channel milestones as three separate posts and always include the exact run label plus raw-event trace URL so DGST-04 remains auditable without leaking into the digest channel. |
| T-01-11 | E | `microclaw.config.yaml.example` / live config | mitigate | Keep Discord channel allowlisting strict and store bot tokens only in env-backed live config, never in git-tracked files. |
| T-01-12 | I | `register_phase1_schedule.ps1` / MicroClaw Web UI | mitigate | Call only `http://127.0.0.1:10961/api/chat`, never a non-local host; fail fast if the API is unreachable instead of falling back to a public endpoint. |
</threat_model>

<verification>
- `uv run pytest tests/test_daily_digest_run.py tests/test_discord_status.py tests/test_ingestion_multisource.py tests/test_run_visibility.py tests/test_run_lineage.py -q`
- `pwsh -NoProfile -File scripts/bootstrap_microclaw_runtime.ps1 -MicroClawBinPath $env:MICROCLAW_BIN_PATH`
- `pwsh -NoProfile -File scripts/register_phase1_schedule.ps1 -WhatIf` completes without syntax errors
</verification>

<success_criteria>
- The workflow has a runnable CLI entrypoint for scheduled, retry, and manual runs.
- Discord-visible narration is limited to the dedicated status channel and exactly three milestones.
- A live local MicroClaw + Ollama runtime can pass health checks and execute one manual Phase 1 smoke run.
- The 08:00 local MicroClaw schedule can be registered automatically or via the printed fallback prompt without adding a second scheduler.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-foundation-run-visibility/01-ingestion-foundation-run-visibility-04-SUMMARY.md`
</output>
